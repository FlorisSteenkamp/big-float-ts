/**
 * * **Dont use!** Use `orien2d` instead.
 * * Only for testing purposes - too slow.
 * @param a
 * @param b
 * @param c
 */
declare function orient2dExact(a: number[], b: number[], c: number[]): number;
export { orient2dExact };
